Set up MySQL database connection and Django settings configuration
● Built predictor app: Prediction model, inference.py, services.py (prediction generation
and persistence)
● Built api app: serializers, DRF endpoints (GET readings, GET predictions, POST
generate-prediction)
● Built and exported the Postman collection with sample requests/responses
● Compiled requirements.txt, README.md, and this contribution sheet
